## 虚拟化总体流程图
```
[ CPU 执行指令，产生虚拟地址 VA ]
                  │
                  ▼
        [ MMU 负责地址翻译 ]
                  │
                  ▼
             [ 先查 TLB ]
                  │
        ┌─────────┴─────────┐
        │                   │
   TLB Hit              TLB Miss
        │                   │
        ▼                   ▼
[ 得到缓存的 PTE ]     [ MMU 进行页表遍历 ]
        │                   │
        │                   ▼
        │          [ 查多级页表中的 PTE ]
        │                   │
        │        ┌──────────┴──────────┐
        │        │                     │
        │   找到有效 PTE          PTE 不存在 /
        │   present=1            present=0 /
        │   权限允许             权限不允许
        │        │                     │
        │        ▼                     ▼
        │   [ 更新 TLB ]          [ 触发 Page Fault 异常 ]
        │        │                     │
        └────────┘                     │
             │                         │
             ▼                         ▼
 [ VA → PA 地址翻译成功 ]       [ CPU 陷入内核态 ]
             │                         │
             ▼                         ▼
 [ 用物理地址 PA 访问缓存层次 ] [ OS 检查该 VA 是否属于合法 VMA ]
             │                         │
     ┌───────┴────────┐        ┌───────┴───────────────┐
     │                │        │                       │
CPU Cache Hit   CPU Cache Miss 合法且可处理        非法或不可修复
     │                │        │                       │
     ▼                ▼        ▼                       ▼
[ 返回数据给 CPU ] [ 访问 RAM ] [ 根据 VMA 类型处理 ] [ 向进程发送 SIGSEGV /
                      │        │                     终止进程 ]
                      ▼        │
              [ 数据装入 Cache ]│
                      │        │
                      ▼        │
              [ 返回数据给 CPU ] │
                               │
        ┌──────────────────────┼────────────────────────┐
        │                      │                        │
        ▼                      ▼                        ▼
 [ 匿名内存页 ]          [ 文件映射页 ]             [ swap 换出页 ]
 brk / malloc / 栈       mmap 文件 / 可执行文件       页面曾在内存中，
        │                代码段 / 共享库              后被换出
        │                      │                        │
        ▼                      ▼                        ▼
[ 分配空闲物理页 ]      [ 查 Page Cache ]          [ 从 swap 读回页面 ]
或映射共享零页                    │                         │
        │              ┌───────┴────────┐                │
        │              │                │                │
        ▼          Page Cache Hit  Page Cache Miss       |
[ 建立 VA → PA 映射 ]       │                │             |
        │                  ▼                ▼            │
        │        [ 找到文件页对应的   [ 发起磁盘 I/O，        │
        │          物理页框 PFN ]       读入 Page Cache ]  │
        │                  │                │            │
        │                  └───────┬────────┘            │
        │                          ▼                     │
        │              [ 建立 VA → 文件页物理页映射 ]        │
        │                                                │
        └──────────────────────┬─────────────────────────┘
                               ▼
                      [ 更新页表 PTE ]
                               │
                               ▼
                      [ 更新 / 刷新 TLB ]
                               │
                               ▼
                   [ 重新执行刚才失败的指令 ]
                               │
                               ▼
                   [ 这次通常走 TLB Hit 路径 ]

```
关于 zero page
```
[ 匿名内存页缺页 ]
        │
        ▼
[ 判断访问类型 ]
        │
   ┌────┴────┐
   │         │
读访问      写访问
   │         │
   ▼         ▼
映射到      分配真实
zero page   物理页
只读        清零
   │         │
   └────┬────┘
        ▼
  更新页表 PTE
```

## 关键名词解释
虚拟地址空间按固定大小切分：页 page
物理内存按固定大小切分：帧 frame / 页框 page frame

> 页是虚拟内存的单位，帧是物理内存的单位。

一般来说，页大小和帧大小一致
